# SQL Server connection (copy to .env and set your values)
# run.sh sources .env if present

export DB_HOST=localhost
export DB_PORT=1433
export DB_USER=sa
export DB_PASSWORD=
export DB_NAME=pipeline

# optional
# export LANDING_DATA_DIR=data/landing
# export PIPELINE_NAME=SamplePipeline
